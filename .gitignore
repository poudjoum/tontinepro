# Java / Spring Boot
target/
*.class
*.war
# Garder .mvn/wrapper/maven-wrapper.properties (tracké) mais ignorer le JAR
.mvn/wrapper/maven-wrapper.jar

# Node / Angular
node_modules/
dist/
.angular/

# Environnement — secrets (ne jamais committer)
.env
*.env.local
application-local.yml
# application-prod.yml est safe (env vars uniquement) → tracké

# IDE
.idea/
*.iml
.vscode/settings.json

# Artefacts de build / travail (ne pas committer)
*.apk
img/

# Documents générés par les scripts docs/ (sources : .html, .py, .js, .md trackés)
docs/TontinePro-*.pdf
docs/TontinePro-*.pptx
docs/TontinePro-*.docx
docs/package.json
docs/package-lock.json

# Logs de lancement local (backend / frontend)
*.log
